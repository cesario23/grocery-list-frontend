import React from 'react'

function index() {
    return (
        <div>
            <button>sort oldest to newest</button>
            <button>sort newest to oldest</button>
            <button>purchased</button>
            <button>not purchased</button>
        </div>
    )
}

export default index
